# === HomeEye master env ===
# Who/where is the Pi?
PI_USER=riplay
PI_HOST=homeeye.local     # you can also put 192.168.0.103 here
PI_SSH_PORT=22

# App port running on the Pi (we picked a low-collision one)
PI_APP_PORT=8787

# Where the repo lives on the Pi
PI_PROJECT_DIR=/home/${PI_USER}/HomeEye

# Optional: fixed IP fallback used by tools that require a numeric IP
# Leave blank to resolve from PI_HOST (mDNS or DNS).
PI_IP=

# Frontend build settings (dev can override with .env.local)
VITE_API_BASE_URL=
VITE_WS_URL=
